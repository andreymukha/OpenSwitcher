# План реализации релиза OpenSwitcher v0.1.1

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Выпустить актуальный `v0.1.1` с Debian-пакетом `0.1.1-1`, исправленными README и проверяемой связью между commit, tag, DEB и SHA-256.

**Architecture:** Runtime-код не меняется. В изолированной ветке обновляются только metadata и документация, после чего из её точного финального commit один раз собирается DEB и проходит единый local/package gate и короткий package-first smoke в двух сохранённых VM. Публикация выполняется только после зелёного кандидата; старый `v0.1.0` не изменяется.

**Tech Stack:** Rust/Cargo, Debian packaging (`dpkg-buildpackage`, `dpkg-deb`, `lintian`), Markdown, Git, GitHub Release, существующая QEMU/KVM VM-лаборатория.

---

## Карта файлов и артефактов

- Modify: `Cargo.toml` — upstream version `0.1.1`.
- Modify: `Cargo.lock` — версия корневого package `open-switcher`.
- Modify: `debian/changelog` — Debian release `0.1.1-1`.
- Modify: `README.md` — актуальная английская документация.
- Modify: `README.ru.md` — смысловой русский эквивалент.
- Create (ignored artifact): `dist/packages/open-switcher_0.1.1-1_amd64.deb`.
- Create (temporary release asset): `/tmp/openswitcher-v0.1.1/SHA256SUMS`.
- Create (temporary release body): `/tmp/openswitcher-v0.1.1/release-notes.md`.
- Reference: `docs/superpowers/specs/2026-08-11-v0.1.1-release-design.md`.
- Reference: `docs/audits/2026-08-11-final-integrated-validation.md`.
- Reference only: `/home/andrey/Projects/OpenSwitcher/dist/final-validation/open-switcher_0.1.0-8_amd64.deb`.
- VM controller: `/home/andrey/Projects/OpenSwitcher/.worktrees/vm-lab`.
- VM state: `/home/andrey/VMs/OpenSwitcherLab` — сохранить, не удалять.

### Task 1: Обновить версии и Debian changelog

**Files:**
- Modify: `Cargo.toml:3`
- Modify: `Cargo.lock:1561-1564`
- Modify: `debian/changelog:1`

- [ ] **Step 1: Подтвердить исходное состояние**

Run:

```bash
rg -n '^version = "0\.1\.0"$' Cargo.toml Cargo.lock
dpkg-parsechangelog -S Version
```

Expected: Cargo показывает `0.1.0`, changelog — `0.1.0-8`; проверка целевых
`0.1.1`/`0.1.1-1` пока не проходит.

- [ ] **Step 2: Изменить Cargo metadata**

В `Cargo.toml` заменить:

```toml
version = "0.1.1"
```

В единственной записи `Cargo.lock`, где `name = "open-switcher"`, установить:

```toml
version = "0.1.1"
```

- [ ] **Step 3: Добавить верхнюю запись changelog**

Перед существующим `0.1.0-8` добавить:

```text
open-switcher (0.1.1-1) unstable; urgency=medium

  * Release the audited input lifecycle and synthetic-input safety fixes.
  * Harden seat-aware device access, package lifecycle, and layout detection.
  * Preserve concurrent clipboard and settings updates on supported paths.
  * Retire the unsupported direct dev runtime and refresh documentation.

 -- Andrey Mukha <6871314+andreymukha@users.noreply.github.com>  Tue, 11 Aug 2026 22:50:00 +0300

```

- [ ] **Step 4: Проверить согласованность версий**

Run:

```bash
cargo metadata --locked --no-deps --format-version 1
dpkg-parsechangelog -S Source
dpkg-parsechangelog -S Version
git diff --check
```

Expected: Cargo metadata содержит `"name":"open-switcher","version":"0.1.1"`;
Debian source равен `open-switcher`, version — `0.1.1-1`, остальные команды
завершаются с кодом `0`.

- [ ] **Step 5: Зафиксировать metadata отдельным коммитом**

```bash
git add Cargo.toml Cargo.lock debian/changelog
git commit -m "chore: prepare v0.1.1 package metadata"
```

### Task 2: Удалить общую устаревшую информацию из обоих README

**Files:**
- Modify: `README.md:21-39,154-169,228-241,394-438,478-493`
- Modify: `README.ru.md:21-40,156-171,231-244,397-441,481-496`
- Reference: `debian/control:19-29`
- Reference: `dist/systemd/open-switcher-xtest-guardian.service`
- Reference: `dist/systemd/open-switcher-xtest-guardian.socket`

- [ ] **Step 1: Зафиксировать RED-проверку устаревших фрагментов**

Run:

```bash
rg -n 'first release|перв(ый|ого) релиз|target/debug|rustfmt drift' README.md README.ru.md
rg -n 'open-switcher-xtest-guardian\.(service|socket)' README.md README.ru.md
```

Expected: первая команда находит устаревшие строки и три прямых запуска в
каждом README; вторая не находит guardian units.

- [ ] **Step 2: Обновить статус и проверенные окружения**

Английский status paragraph заменить на:

```markdown
OpenSwitcher is a Linux desktop application for EN/RU typing workflows, built around a
`daemon + tray` runtime model.
```

Русский эквивалент:

```markdown
OpenSwitcher — настольное Linux-приложение для EN/RU-сценариев ввода,
построенное вокруг модели `daemon + tray`.
```

В обеих таблицах заменить прежнее `local smoke testing` на соответственно:

```text
Confirmed by exact-DEB VM testing
Подтверждено проверкой exact DEB в ВМ
```

- [ ] **Step 3: Актуализировать ограничения**

В обоих повторяющихся списках использовать смысловые пары:

```text
Application-specific exclusions are not currently available
A GNOME Shell extension is not included in the package

Исключения для отдельных приложений пока не поддерживаются
GNOME Shell extension не входит в пакет
```

Сохранить остальные реальные ограничения без изменения.

- [ ] **Step 4: Точно описать clipboard policy**

После последовательности selected-text conversion в `README.md` добавить:

```markdown
When the previous clipboard contains text and no other application changes it during the
operation, OpenSwitcher restores that text on supported success and safe failure paths. Desktop
clipboard protocols do not provide an atomic compare-and-swap, so a short race window remains.
If the previous clipboard contains a non-text format such as an image, a successful conversion
intentionally leaves the converted text in the clipboard instead of blocking the conversion.
```

В `README.ru.md` добавить:

```markdown
Если до операции clipboard содержал текст и другое приложение не изменило его во время
конвертации, OpenSwitcher восстанавливает этот текст на поддерживаемых успешных и безопасных
ошибочных путях. Desktop clipboard protocols не предоставляют атомарный compare-and-swap,
поэтому остаётся короткое race-окно. Если до операции clipboard содержал нетекстовый формат,
например изображение, успешная конвертация намеренно оставляет в нём преобразованный текст,
а не блокирует операцию.
```

В `Known Limitations`/`Известные ограничения` заменить краткую clipboard-строку
на ссылку на эту точную политику без обещания безусловного restore.

- [ ] **Step 5: Исправить systemd assets и developer workflow**

В список distribution assets после tray unit добавить:

```markdown
- `dist/systemd/open-switcher-xtest-guardian.socket`
- `dist/systemd/open-switcher-xtest-guardian.service`
```

В русском README добавить те же пути. Полностью удалить разделы
`## Direct Binary Runs` и `## Прямой запуск бинарников` вместе с командами
`./target/debug/open-switcher*`.

В Quick Start заменить неточную фразу на смысловую пару:

```text
Direct launches of `target/` binaries are not supported.
Прямой запуск бинарников из `target/` не поддерживается.
```

- [ ] **Step 6: Убрать устранённый drift, сохранив реальное ограничение**

Последнюю строку Known Limitations заменить на:

```text
Some shell/platform tests depend on Linux session services and cannot run hermetically in every restricted sandbox.
Некоторые shell/platform tests зависят от служб пользовательской Linux-сессии и не могут выполняться hermetic во всех restricted sandbox.
```

- [ ] **Step 7: Проверить обе версии README одним gate**

Run:

```bash
! rg -n 'first release|перв(ый|ого) релиз|target/debug|rustfmt drift' README.md README.ru.md
test "$(rg -c 'open-switcher-xtest-guardian\.(service|socket)' README.md)" -eq 2
test "$(rg -c 'open-switcher-xtest-guardian\.(service|socket)' README.ru.md)" -eq 2
test "$(rg -c '^```' README.md)" -eq "$(rg -c '^```' README.ru.md)"
test "$(rg -c '^#{1,6} ' README.md)" -eq "$(rg -c '^#{1,6} ' README.ru.md)"
for dep in x11-xkb-utils libglib2.0-bin gsettings-desktop-schemas; do rg -q "$dep" debian/control; rg -q "$dep" README.md; rg -q "$dep" README.ru.md; done
git diff --check
```

Затем выполнить:

```bash
awk '/^```/{inside=!inside; next} inside && $0 !~ /^#/ {print}' README.md > /tmp/openswitcher-readme-en-blocks
awk '/^```/{inside=!inside; next} inside && $0 !~ /^#/ {print}' README.ru.md > /tmp/openswitcher-readme-ru-blocks
diff -u /tmp/openswitcher-readme-en-blocks /tmp/openswitcher-readme-ru-blocks
test -f README.md
test -f README.ru.md
rg -q '^## Tested Environments$' README.md
rg -q '^## Проверенные окружения$' README.ru.md
```

Expected: командных расхождений нет; локальные файлы и anchors существуют.

- [ ] **Step 8: Зафиксировать README отдельным коммитом**

```bash
git add README.md README.ru.md
git commit -m "docs: refresh release and runtime guidance"
```

### Task 3: Выполнить один итоговый source/package gate и собрать DEB

**Files:**
- Verify: весь tracked release candidate
- Create (ignored): `dist/packages/open-switcher_0.1.1-1_amd64.deb`
- Create (ignored): `dist/packages/open-switcher-dbgsym_0.1.1-1_amd64.ddeb`

- [ ] **Step 1: Доказать отсутствие runtime-изменений**

Run:

```bash
git status --short
git diff --quiet de2bdd75ef5a235bb0497e3d541cfa191ecfd94e -- src vendor manage.sh dist/systemd debian/control debian/rules debian/open-switcher.install debian/open-switcher.preinst debian/open-switcher.postinst debian/open-switcher.prerm debian/open-switcher.postrm
git diff --name-status de2bdd75ef5a235bb0497e3d541cfa191ecfd94e..HEAD
```

Expected: tracked tree чист; runtime/package implementation diff пуст; список
изменений содержит только spec/plan, Cargo metadata, changelog и два README.

- [ ] **Step 2: Выполнить недублирующиеся предварительные checks**

Run последовательно:

```bash
cargo fmt --check
cargo check --locked --all-targets --features settings-ui
cargo test --locked --manifest-path vendor/uinput-0.1.3/Cargo.toml
bash tests/linux_input_setup_test.sh
bash tests/manage_dev_retirement_test.sh
bash tests/manage_package_deb_test.sh
```

Expected: все команды exit `0`; предупреждения deprecated API vendored
`uinput 0.1.3` допустимы, новые errors/failures — нет.

- [ ] **Step 3: Один раз собрать канонический пакет**

Run вне restricted syscall sandbox, чтобы уже известные Unix-socket tests не
зависли на запрещённом `shutdown(2)`:

```bash
./manage.sh package deb
```

Expected: package build сам выполняет основную Rust matrix, settings-ui library
matrix, `wayland_diagnostics`, `input_access_package`,
`debian_package_scripts`, desktop validation и `lintian`; все обязательные
checks проходят, а итоговый путь равен
`dist/packages/open-switcher_0.1.1-1_amd64.deb`.

Если окружение не разрешает полный test gate, сборка с `nocheck` не считается
приемлемой сама по себе: те же exact test binaries сначала должны пройти в
сохранённой VM, после чего разрешена одна artifact-only сборка с
`DEB_BUILD_OPTIONS=nocheck`.

### Task 4: Проверить exact package и подготовить release assets

**Files:**
- Verify: `dist/packages/open-switcher_0.1.1-1_amd64.deb`
- Reference: `/home/andrey/Projects/OpenSwitcher/dist/final-validation/open-switcher_0.1.0-8_amd64.deb`
- Create: `/tmp/openswitcher-v0.1.1/SHA256SUMS`
- Create: `/tmp/openswitcher-v0.1.1/release-notes.md`

- [ ] **Step 1: Проверить metadata и обязательный payload**

Run:

```bash
dpkg-deb -f dist/packages/open-switcher_0.1.1-1_amd64.deb Package Version Architecture
dpkg-deb -c dist/packages/open-switcher_0.1.1-1_amd64.deb
```

Expected: `open-switcher`, `0.1.1-1`, `amd64`; payload содержит три binaries,
udev rule, desktop/autostart/icon, четыре helpers и ровно четыре user units:
daemon, tray, guardian service и guardian socket.

- [ ] **Step 2: Сравнить структуру с проверенным `0.1.0-8`**

Для старого и нового DEB получить отсортированные пары `mode + path` из
`dpkg-deb -c` и сравнить через `diff -u`. Отдельно извлечь control archives и
сравнить `preinst`, `postinst`, `prerm`, `postrm`; извлечь payloads и сравнить
udev rule, desktop files, icon, helpers и systemd units.

Expected: path/mode manifests и перечисленные package-owned файлы совпадают;
различаются только ожидаемые package metadata, compressed changelog и
пересобранные Rust binaries.

- [ ] **Step 3: Создать и проверить checksum asset**

Run:

```bash
mkdir -p /tmp/openswitcher-v0.1.1
cd dist/packages
sha256sum open-switcher_0.1.1-1_amd64.deb > /tmp/openswitcher-v0.1.1/SHA256SUMS
sha256sum --check /tmp/openswitcher-v0.1.1/SHA256SUMS
```

Expected: ровно одна строка и `open-switcher_0.1.1-1_amd64.deb: OK`.

- [ ] **Step 4: Подготовить русский release body**

Создать `/tmp/openswitcher-v0.1.1/release-notes.md` с:

- названием `OpenSwitcher v0.1.1`;
- кратким итогом аудита и исправлений input lifecycle, synthetic-input,
  seat-aware access, layout, clipboard, settings и package lifecycle;
- проверенными Mint/Cinnamon/X11 и Ubuntu/GNOME/Wayland;
- командой `sudo apt install ./open-switcher_0.1.1-1_amd64.deb`;
- точным SHA-256 из `SHA256SUMS`;
- сохранёнными ограничениями EN/RU scope, остальных окружений best-effort и
  suspend/resume на реальном оборудовании.

Не включать неподтверждённые заявления и не прикладывать `.ddeb` как
обязательный пользовательский asset.

### Task 5: Выполнить короткий smoke exact DEB в двух VM

**Files/State:**
- Use: `/home/andrey/Projects/OpenSwitcher/.worktrees/vm-lab`
- Use: `/home/andrey/VMs/OpenSwitcherLab/runs/mint-install-v1/disk.qcow2`
- Use: `/home/andrey/VMs/OpenSwitcherLab/runs/ubuntu-cloud-provision-v1/disk.qcow2`
- Preserve: все VM disks, keys и прежнее evidence

- [ ] **Step 1: Проверить границу лаборатории**

До запуска подтвердить отсутствие QEMU-процессов, listeners `22222/22223` и
stale QMP sockets. Не использовать bridge/TAP, shared folders, shared clipboard
или passthrough host input. Запускать гостей последовательно.

- [ ] **Step 2: Проверить Mint/Cinnamon/X11**

Из VM-lab worktree запустить:

```bash
python3 -m tools.vm_lab.session mint-installed
```

Через SSH `127.0.0.1:22223` с ключом
`/home/andrey/VMs/OpenSwitcherLab/keys/id_ed25519` и строгим
`known_hosts` передать exact DEB, сверить его SHA-256 и выполнить внутри гостя:

```bash
sudo apt-get install --yes /tmp/open-switcher_0.1.1-1_amd64.deb
dpkg-query -W -f='${Package} ${Version} ${Architecture}\n' open-switcher
systemctl --user is-active open-switcher-daemon.service open-switcher-tray.service open-switcher-xtest-guardian.socket
systemctl --user show -p NRestarts open-switcher-daemon.service open-switcher-tray.service
```

Через уже применявшийся QMP virtual keyboard открыть тестовый editor, передать
physical keys `s`, `u`, `d`, `o`, затем `F12` при RU layout и подтвердить
результат `sudo`. Остановка/запуск daemon должны завершиться штатно, после smoke
failed user units и restart loop отсутствуют.

- [ ] **Step 3: Проверить Ubuntu/GNOME/Wayland**

Штатно выключить Mint, затем запустить:

```bash
python3 -m tools.vm_lab.session ubuntu-installed
```

Повторить передачу, SHA/install/service checks через `127.0.0.1:22222` и тот же
строгий ключевой контур. Выполнить тот же virtual-keyboard F12 smoke и получить
`sudo`. Подтвердить GNOME/Wayland, версию `0.1.1-1`, active daemon/tray,
`NRestarts=0` и отсутствие failed user units.

- [ ] **Step 4: Завершить VM gate**

Штатно выключить Ubuntu. Подтвердить отсутствие QEMU-процессов, listeners и
QMP sockets. Диски, лабораторию и прежнее evidence не удалять и не откатывать.
Если smoke выявил runtime defect, остановить релиз до отдельного regression
test/fix; не публиковать tag или GitHub Release.

### Task 6: Финальная проверка, интеграция и публикация

**Files/State:**
- Merge: `release/v0.1.1` -> `master`
- Tag: `v0.1.1`
- Publish: GitHub Release assets DEB + `SHA256SUMS`

- [ ] **Step 1: Выполнить финальный review gate**

Применить `superpowers:requesting-code-review` и
`superpowers:verification-before-completion`. Проверить:

```bash
git status --short
git diff --check de2bdd75ef5a235bb0497e3d541cfa191ecfd94e..HEAD
git diff --stat de2bdd75ef5a235bb0497e3d541cfa191ecfd94e..HEAD
git log --oneline de2bdd75ef5a235bb0497e3d541cfa191ecfd94e..HEAD
```

Expected: tracked tree чист; runtime source не менялся; все commits ограничены
design, plan, versions/changelog и README; exact artifact/hash/VM evidence
относятся к текущему `HEAD`.

- [ ] **Step 2: Интегрировать ветку без потери пользовательских файлов**

В основном worktree убедиться, что remote `master` всё ещё указывает на
`de2bdd75ef5a235bb0497e3d541cfa191ecfd94e`, затем выполнить fast-forward merge
ветки `release/v0.1.1`. Не добавлять пользовательское изменение `.gitignore`,
старые untracked audit-документы или `dist/final-validation/`.

- [ ] **Step 3: Сохранить пользовательский release artifact**

Скопировать exact DEB и `SHA256SUMS` в основной
`/home/andrey/Projects/OpenSwitcher/dist/packages/`, затем повторить
`sha256sum --check` из этого каталога. Показать пользователю точный путь и
команду установки; host package автоматически не переустанавливать.

- [ ] **Step 4: Push, CI и tag**

Отправить `master` в `origin`, дождаться зелёного GitHub Actions run для exact
release commit, затем создать и отправить аннотированный tag:

```bash
RELEASE_COMMIT="$(git rev-parse HEAD)"
git tag -a v0.1.1 -m "OpenSwitcher v0.1.1" "$RELEASE_COMMIT"
git push origin v0.1.1
```

Перед tag повторно убедиться, что `v0.1.1` отсутствует локально и в GitHub.
Если CI красный, tag не создавать.

- [ ] **Step 5: Создать GitHub Release**

При действующей `gh`-авторизации выполнить:

```bash
gh release create v0.1.1 \
  /home/andrey/Projects/OpenSwitcher/dist/packages/open-switcher_0.1.1-1_amd64.deb \
  /home/andrey/Projects/OpenSwitcher/dist/packages/SHA256SUMS \
  --verify-tag \
  --title "OpenSwitcher v0.1.1" \
  --notes-file /tmp/openswitcher-v0.1.1/release-notes.md
```

Если локальный `gh` по-прежнему не авторизован, не передавать credentials
ассистенту: показать пользователю эту exact команду для выполнения в его
терминале.

- [ ] **Step 6: Проверить опубликованный релиз read-only**

Через публичный GitHub API сверить:

- tag `v0.1.1` указывает на exact release commit;
- release не draft и не prerelease;
- присутствуют только основной DEB и `SHA256SUMS`;
- имя, размер и SHA-256 DEB совпадают с локальным кандидатом;
- старый `v0.1.0` и его asset не изменились.

После этого обновить план как завершённый. Временный release worktree можно
удалить по процедуре finishing-branch; VM-лабораторию не удалять.
